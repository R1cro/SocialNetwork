<%= form_for(@micropost, html: { multipart: true }) do |f| %>
    <%= render 'shared/error_messages', object: f.object %>
    <div class="post-field">
      <%= f.text_area :content, placeholder: 'Compose new micropost...', class: 'post-field' %>
      <%= f.text_field :tag_list, placeholder: '#HashTag' %>
    </div>
    <span class="image">
      <%= f.file_field(:image) %>
    </span>
    <%= f.submit 'Post', class: 'btn btn-primary' %>
<% end %>