<% if @feed_items.any? %>
    <h4>Last users posts</h4>
    <ol class="microposts">
      <%= render @feed_items %>
    </ol>
    <% if logged_in? %>
      <%= will_paginate @feed_items %>
    <% end %>
<% end %>
